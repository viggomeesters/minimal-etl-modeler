// Script to move blocks to better positions for demonstration
const blocks = document.querySelectorAll('.block');
if (blocks[0]) {
    blocks[0].style.left = '100px';
    blocks[0].style.top = '150px';
}
if (blocks[1]) {
    blocks[1].style.left = '400px';
    blocks[1].style.top = '150px';
}
if (blocks[2]) {
    blocks[2].style.left = '700px';
    blocks[2].style.top = '150px';
}
